# MOS web data boundary

The web UI must consume only data that has passed the MOS data model and Safety Gate.

Do not place raw social posts, unverified claims, or manually typed emergency facts in the frontend.

Future API/static data generation must expose claim IDs, verification state, risk, source count, timestamps, and original source URLs.
