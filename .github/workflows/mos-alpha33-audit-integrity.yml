name: MOS Alpha-33 Audit Integrity
on:
  push:
    branches: [mos-alpha33-audit-integrity]
  workflow_dispatch:
jobs:
  audit-integrity-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python -m pip install --upgrade pip pytest
      - run: python -m pytest -q tests/test_mos_alpha33_audit_integrity.py
